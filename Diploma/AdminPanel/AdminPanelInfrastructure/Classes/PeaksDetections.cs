using System;
using System.Collections.Generic;
using System.Text;
using System.Threading.Tasks;

namespace Server.Infrastructure.Classes
{
    public static class PeaksDetection
    {
        //public static Task Wavelet(List<string> Points)
        //{
        //    int fs = 360;
        //    double f1 = 0.5, f2 = 35;
        //    double[] Wn = new double[] { f1 * 2 / fs, f2 * 2 / fs };

        //    double N = 3;
        //    var ab = (0, 0);

            

        //}

        //private static double[] Buttap(double n)
        //{
        //    double z[]


        //    double[] zpk = { 1, 2, 3 };
        //    return zpk;
        //}

        //private static double[] ButterWorth(double n, double[] Wn)
        //{
        //    int fs = 2;
        //    double[] u = new double[] { 2 * fs * Math.Tan(Math.PI * Wn[0] / fs), 2 * fs * Math.Tan(Math.PI * Wn[1] / fs) };
        //    int bType = 2;
        //    double Bw = u[1] - u[0];
        //    double wn = Math.Sqrt(u[0] * u[2]);

        //    double[] zpk = Buttap(n);
        //    double z = zpk[0];
        //    double p = zpk[1];
        //    double k = zpk[2];
            
        }
    }
