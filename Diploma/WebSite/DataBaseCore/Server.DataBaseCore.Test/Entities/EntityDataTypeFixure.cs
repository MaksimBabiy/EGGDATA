namespace Server.DataBaseCore.Test.Entities
{
    using System;
    using Server.DataBaseCore.Entities;
    using Xunit;

    public class EntityDataTypeFixure
    {
        [Fact]
        public void ToModelEntity_SuccessAdding()
        {
            //Patient patient = new Patient { IsActive = true, CreatedDate = DateTime.Now, UpdatedDate = DateTime.Now };

            //Patient patient2 = new Patient();

            //patient2.ToModelEntity(patient);

            //Assert.Equal(patient2.IsActive, patient.IsActive);
            //Assert.Equal(patient2.CreatedDate, patient.CreatedDate);
            //Assert.Equal(patient2.UpdatedDate, patient.UpdatedDate);
        }
    }
}
